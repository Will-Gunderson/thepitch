---
import Base from '../layouts/Base.astro';
// Body is the Webflow export's markup, held verbatim in src/content/amenities.html and
// injected with set:html. It is NOT inlined as Astro markup on purpose: Webflow nests
// <script class="w-json"> (lightbox config) inside <a class="w-lightbox">, and Astro's
// parser treats the <a> as an unclosed formatting element and reconstructs it around
// everything that follows — on amenities that turned 18 anchors into 157 and wrecked
// the DOM. set:html keeps the bytes as Webflow wrote them.
import rawBody from '../content/amenities.html?raw';
// The unit-amenity badges live in one file and are injected into both the pages that
// show them. Webflow exported this CMS collection populated on amenities and empty on
// floor-plans, which is why the floor plans page had the heading and intro line but no
// badges at all. Sharing the markup means that can't happen again.
import badges from '../content/_unit-amenity-badges.html?raw';
const body = rawBody.replace('<!--UNIT_AMENITY_BADGES-->', badges);
---
<Base title="Amenities | The Pitch Apartments, St. Paul" description="Elevated amenity deck with firepit and cedar-lined sauna, second-floor club room, fitness center, coworking lounge, bike storage and package lockers." wfPage="60ff77fe2e82b3ee85a926a9" current="amenities">
  <Fragment set:html={body} />
</Base>
